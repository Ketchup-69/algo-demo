<!-- PLACEHOLDER: replace when the brand mark is approved. Deliberately not a
     logo — a neutral token-derived tile, so nothing invented ships as identity. -->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="32" height="32">
  <defs>
    <linearGradient id="a" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0" stop-color="#72c9f4"/>
      <stop offset="1" stop-color="#57e0dc"/>
    </linearGradient>
  </defs>
  <rect width="32" height="32" rx="7" fill="#17243f"/>
  <rect x="8" y="8" width="16" height="16" rx="4" fill="url(#a)"/>
</svg>
